# issue 12: Discount endpoint subtracts percent as a fixed amount

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/12
- Conclusion: Confirmed bug: the discount logic subtracts the percent value as a flat amount.
- Labels: bug, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/17
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28037703404
- Updated: 2026-06-23T15:40:55.721Z

```json
{
  "triage": {
    "conclusion": "Confirmed bug: the discount logic subtracts the percent value as a flat amount.",
    "summary": "The /discount endpoint calls calculateDiscountedPrice, which currently returns price - discountPercent instead of applying discountPercent as a percentage of price.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed. `src/app.js` passes the query parameters to `calculateDiscountedPrice`, and `src/business.js` currently subtracts `discountPercent` directly from `price`. For `price=250&percent=10`, that produces `240` instead of the expected `225`. There is already a test in `test/business.test.js` asserting `calculateDiscountedPrice(250, 10) === 225`, so this looks like a small, well-scoped bug fix.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md",
        "package.json"
      ],
      "findings": [
        "`src/app.js` implements `GET /discount` by parsing `price` and `percent` from the query string and returning `{ price: calculateDiscountedPrice(price, percent) }`.",
        "`src/business.js` contains `calculateDiscountedPrice(price, discountPercent)`, which currently returns `roundToCents(price - discountPercent)` for positive discounts.",
        "`src/business.js` also has an inline comment explicitly noting the bug: it treats a percentage discount as a flat currency amount.",
        "`test/business.test.js` already includes `discount calculation applies a percentage discount` and expects `calculateDiscountedPrice(250, 10)` to equal `225`.",
        "`README.md` lists this exact behavior under dogfood issue ideas: `Discount calculation treats the percent as a flat amount.`"
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The bug is isolated to a single business function, the expected behavior is clear from the report and existing test, and the endpoint already passes the needed values through correctly.",
      "suggestedApproach": "Change `calculateDiscountedPrice` so positive discounts subtract `price * (discountPercent / 100)` from `price`, then round with the existing `roundToCents` helper. Keep the zero-or-negative discount behavior unchanged.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "a8bc5c479c76730f222110f28b9782edc8b5400e9634999770af3398a105dc7f",
  "piCalls": 3,
  "runId": 28037703404,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28037703404"
}
```
