# issue 26: Cart total ignores line item quantities

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/26
- Conclusion: Valid bug: cart total ignores line item quantities
- Labels: bug, good-first-issue, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/32
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28363722282
- Updated: 2026-06-29T09:59:57.485Z

```json
{
  "triage": {
    "conclusion": "Valid bug: cart total ignores line item quantities",
    "summary": "POST /cart/total returns the sum of item prices only, undercounting items whose quantity is greater than 1.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug",
      "good-first-issue"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear repro. I verified this against the current code: `POST /cart/total` calls `totalCart(body.items || [])`, and `totalCart` currently sums only `item.price` for each line item. That matches the reported actual result of `5.5` for the example payload, while the existing focused test expects `17` when quantities are included.\n\nThis looks like a small, isolated fix in `src/business.js`: each line item should contribute `item.price * item.quantity` before rounding. Risk appears low because the endpoint delegates directly to this helper and `test/business.test.js` already covers the expected behavior.",
    "investigation": {
      "relevantFiles": [
        "src/business.js",
        "src/app.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/business.js` implements `totalCart` as `items.reduce((total, item) => total + item.price, 0)`, which ignores `item.quantity`.",
        "`src/app.js` handles `POST /cart/total` by passing `body.items || []` directly to `totalCart`.",
        "`test/business.test.js` has a focused test named `cart total includes item quantities` using the same prices and quantities and expecting `17`.",
        "`README.md` lists `Cart total ignores item quantities` as one of the intended issue scenarios."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The bug is isolated to one helper function and already has a direct test describing the expected behavior.",
      "suggestedApproach": "Update `src/business.js` so `totalCart` sums `item.price * item.quantity` for each line item, preserving the existing final `roundToCents` behavior and avoiding unrelated changes.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 32,
      "type": "pull_request",
      "state": "open",
      "title": "Fix #26: Cart total ignores line item quantities",
      "url": "https://github.com/marandaneto/bug-project-watcher/pull/32",
      "labels": [],
      "bodyExcerpt": "Fixes #26\n\nGenerated by posthog-watcher-action using pi.\n\n## Summary\n\nThe `/cart/total` endpoint delegates to `totalCart`, which currently sums each line item's price once instead of multiplying by quantity.\n\n## Triage rationale\n\nThe bug is isolated to the `totalCart` calculation and there is already a direct test covering the expected behavior.\n\n## Changed files\n\n- `src/business.js`\n\n## Validation\n\n- `node -e \"require('./src/business'); require('./src/app'); console.log('modules load')\"`\n",
      "createdAt": "2026-06-23T18:13:41Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "f98f46d2cf5215371682a1992c2795e040eea172d80d50976f253b1ec1368ed2",
  "piCalls": 11,
  "runId": 28363722282,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28363722282"
}
```
