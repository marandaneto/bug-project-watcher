# issue 11: Email validation accepts addresses without a domain suffix

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/11
- Conclusion: Valid bug report; email validation is too permissive.
- Labels: bug, good-first-issue, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/15
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28037706330
- Updated: 2026-06-23T15:40:41.079Z

```json
{
  "triage": {
    "conclusion": "Valid bug report; email validation is too permissive.",
    "summary": "The `/users/validate-email` endpoint delegates to a regex that only requires an `@`, so `person@example` is currently accepted despite lacking a dotted domain suffix.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug",
      "good-first-issue"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I verified the relevant path in the repository: `/users/validate-email` calls `isValidEmail`, and that function currently uses `/^\\S+@\\S+$/`, which accepts `person@example` because it only checks for non-whitespace on both sides of `@`.\n\nThere is already a focused test in `test/business.test.js` expecting `isValidEmail('person@example')` to be `false`, so this looks like a confirmed, small validation bug. A minimal fix would be to tighten the email regex to require a dot in the domain portion, without attempting a full RFC-compliant email parser.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` routes `POST /users/validate-email` to `isValidEmail(body.email || '')`.",
        "`src/business.js` implements `isValidEmail` as `/^\\S+@\\S+$/`, which accepts `person@example` because it does not require a domain suffix.",
        "`test/business.test.js` already contains a test asserting that `isValidEmail('person@example')` should be `false` and that `person@example.com` should be `true`.",
        "`README.md` lists this exact behavior as one of the known dogfood issue ideas."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The issue is isolated to a single helper function and already has focused tests describing the expected behavior. No API shape change is needed.",
      "suggestedApproach": "Update `isValidEmail` in `src/business.js` to require a non-empty local part, an `@`, and a domain containing at least one dot with non-whitespace characters on both sides, for example a simple pattern like `/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/`. Keep the existing tests as verification.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "861b46d637e82c49da7f2f4ee16f8289480844c288e6a6d26d6d9d5157a9a6e5",
  "piCalls": 3,
  "runId": 28037706330,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28037706330"
}
```
