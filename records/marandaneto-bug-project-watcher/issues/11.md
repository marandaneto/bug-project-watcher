# issue 11: Email validation accepts addresses without a domain suffix

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/11
- Conclusion: Valid bug report; email validation is too permissive.
- Labels: bug, good-first-issue, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/15
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28037706330
- Updated: 2026-06-23T15:50:20.304Z

```json
{
  "triage": {
    "conclusion": "Valid bug report; email validation is too permissive.",
    "summary": "The `/users/validate-email` endpoint accepts `person@example` because the shared email validator only checks for non-whitespace text around `@` and does not require a domain suffix.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug",
      "good-first-issue"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I verified the relevant code path: `POST /users/validate-email` calls `isValidEmail`, and `src/business.js` currently uses `/^\\S+@\\S+$/`, which accepts `person@example` because it only requires non-whitespace text before and after `@`.\n\nThere is already a focused test in `test/business.test.js` expecting `isValidEmail('person@example')` to return `false`, while accepting `person@example.com`. This is a small, well-scoped bug. A minimal fix would be to tighten the validator to require a dot in the domain portion, e.g. a simple pattern like `/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/`, without trying to implement full RFC email validation.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` routes `POST /users/validate-email` to `isValidEmail(body.email || '')`.",
        "`src/business.js` implements `isValidEmail` as `/^\\S+@\\S+$/`, so `person@example` matches because the regex does not require a domain suffix.",
        "`test/business.test.js` already contains a test asserting `isValidEmail('person@example')` should be `false` and `isValidEmail('person@example.com')` should be `true`.",
        "`README.md` lists \"Email validation accepts addresses without a domain suffix\" as a known dogfood issue."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The issue is isolated to a single helper function, the expected behavior is clearly covered by existing tests, and no API shape change is required.",
      "suggestedApproach": "Update `isValidEmail` in `src/business.js` to require a non-empty local part, an `@`, and a domain containing at least one dot with non-whitespace/non-`@` characters on both sides, such as `/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/`. Use the existing business tests as verification.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 15,
      "type": "pull_request",
      "state": "open",
      "title": "Fix #11: Email validation accepts addresses without a domain suffix",
      "url": "https://github.com/marandaneto/bug-project-watcher/pull/15",
      "labels": [],
      "bodyExcerpt": "Fixes #11\n\nGenerated by posthog-watcher-action using pi.\n\n## Summary\n\nThe `/users/validate-email` endpoint delegates to a regex that only requires an `@`, so `person@example` is currently accepted despite lacking a dotted domain suffix.\n\n## Triage rationale\n\nThe issue is isolated to a single helper function and already has focused tests describing the expected behavior. No API shape change is needed.\n\n## Changed files\n\n- `src/business.js`\n\n## Validation\n\n- `node -e \"require('./src/business'); require('./src/app'); console.log('modules load')\"`\n",
      "createdAt": "2026-06-23T15:40:39Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "56b2ede8c3cc68747adcd552846718f6d9cac6dd2ab50ce9625878502f03ce6f",
  "piCalls": 4,
  "runId": 28037706330,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28037706330"
}
```
