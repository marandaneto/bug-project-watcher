# issue 8: Cart total does not include item quantities

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/8
- Conclusion: Valid bug: cart totals ignore item quantities.
- Labels: bug, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/16
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28037706576
- Updated: 2026-06-23T15:40:42.235Z

```json
{
  "triage": {
    "conclusion": "Valid bug: cart totals ignore item quantities.",
    "summary": "The /cart/total endpoint delegates to totalCart, which currently sums only item.price and does not multiply by quantity, matching the reported undercount.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I verified this against the repository code: `src/app.js` calls `totalCart(body.items || [])` for `POST /cart/total`, and `src/business.js` currently implements `totalCart` as a sum of `item.price` only. There is also already a test in `test/business.test.js` expecting the sample cart to total `17`, so this is a well-scoped bug.\n\nA minimal fix would be to update `totalCart` to add `item.price * item.quantity` for each line item, preserving the existing cents rounding behavior.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` routes `POST /cart/total` to `totalCart(body.items || [])`, so the endpoint behavior is controlled by `src/business.js`.",
        "`src/business.js` defines `totalCart(items)` as `items.reduce((total, item) => total + item.price, 0)`, which ignores `item.quantity`.",
        "`test/business.test.js` already contains a focused test named `cart total includes item quantities` expecting the reported input to return `17`.",
        "`README.md` lists `Cart total ignores item quantities` as one of the known dogfood issue ideas."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The bug is localized to one small function, the expected behavior is clearly specified, and a matching test already exists.",
      "suggestedApproach": "Change `totalCart` in `src/business.js` to sum `item.price * item.quantity` for each item, keeping the existing `roundToCents` wrapper. Then run `npm test` to verify the existing cart test passes.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "6cb5f21feb845124bc4dd093b48c1ee0c1993488aa34f6792beeab3e4723f4fc",
  "piCalls": 3,
  "runId": 28037706576,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28037706576"
}
```
