# issue 10: Shipping estimate drops cents from the calculated price

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/10
- Conclusion: Valid bug: shipping calculation floors the estimate and drops cents.
- Labels: bug, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/14
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28037705214
- Updated: 2026-06-23T15:40:24.123Z

```json
{
  "triage": {
    "conclusion": "Valid bug: shipping calculation floors the estimate and drops cents.",
    "summary": "The /shipping endpoint delegates to estimateShipping, which currently returns Math.floor(estimate), so a 2.2kg package returns 7 instead of the expected 7.75.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear reproduction. I verified this against the code: `src/business.js` computes `5 + weightKg * 1.25` and then returns `Math.floor(estimate)`, which intentionally drops the cents. For `weightKg=2.2`, the calculated estimate is `7.75`, but flooring returns `7`.\n\nA minimal fix would be to return the existing cents-preserving rounding helper result instead of flooring, and keep the existing shipping test expectation of `estimateShipping(2.2) === 7.75`.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` handles `GET /shipping` by parsing `weightKg` and returning `{ shipping: estimateShipping(weightKg, expedited) }`.",
        "`src/business.js` calculates shipping as `5 + weightKg * 1.25`, applies an expedited multiplier if requested, then returns `Math.floor(estimate)`, which drops cents.",
        "For the reported input `2.2`, the formula produces `5 + 2.2 * 1.25 = 7.75`, but `Math.floor(7.75)` returns `7`.",
        "`test/business.test.js` already contains a test named `shipping estimate preserves cents` expecting `estimateShipping(2.2)` to equal `7.75`.",
        "`README.md` lists 'Shipping estimates round down instead of preserving cents' as one of the known dogfood bug scenarios."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The faulty behavior is isolated to one return statement in `estimateShipping`; there is already a helper for rounding to cents and an existing test capturing the expected behavior.",
      "suggestedApproach": "Change `estimateShipping` in `src/business.js` to return `roundToCents(estimate)` instead of `Math.floor(estimate)`, then run `npm test`.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "6781669c1b4f9362556f83c6e2875aa417c9dfd8bcc415f069751d8f90f25206",
  "piCalls": 3,
  "runId": 28037705214,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28037705214"
}
```
