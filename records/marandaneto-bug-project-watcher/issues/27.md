# issue 27: Normalized usernames keep repeated whitespace

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/27
- Conclusion: Confirmed bug in username normalization
- Labels: bug, good-first-issue, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/31
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28047000844
- Updated: 2026-06-23T18:13:33.267Z

```json
{
  "triage": {
    "conclusion": "Confirmed bug in username normalization",
    "summary": "The /users/normalize endpoint uses normalizeUsername, which trims and lowercases but only replaces the first literal space, leaving repeated whitespace in normalized usernames.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug",
      "good-first-issue"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear repro. This is confirmed in `src/business.js`: `normalizeUsername` currently calls `.replace(' ', '-')`, which replaces only the first literal space and does not collapse repeated whitespace. There is also an existing test in `test/business.test.js` expecting `normalizeUsername('  Ada   Lovelace  ')` to return `ada-lovelace`.\n\nA small fix should be to replace one-or-more whitespace runs with a single hyphen after trimming/lowercasing, e.g. using a global whitespace regex.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` routes `POST /users/normalize` to `normalizeUsername(body.username || '')`, so the reported endpoint behavior comes directly from the business function.",
        "`src/business.js` implements `normalizeUsername` as `username.trim().toLowerCase().replace(' ', '-')`, which only replaces the first literal space.",
        "`test/business.test.js` already contains a test named `username normalization trims, lowercases, and collapses whitespace` expecting `ada-lovelace` for the reported input.",
        "`README.md` lists this as a known dogfood bug: username normalization only replaces the first space and leaves repeated whitespace."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The bug is localized to one small function, the expected behavior is clear from the issue and existing test, and the minimal change is to replace runs of whitespace globally with a single hyphen.",
      "suggestedApproach": "Update `normalizeUsername` in `src/business.js` to trim and lowercase as before, then replace one or more whitespace characters with a single hyphen, for example `.replace(/\\s+/g, '-')`. Verify with the existing username normalization test.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "197ad85dd52b59481ec95f9242150114951dfbc7cfaaf51f977c3ad64153d233",
  "piCalls": 3,
  "runId": 28047000844,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28047000844"
}
```
