# issue 5: Discount API is not applying percentage values correctly

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/5
- Conclusion: Valid bug: the discount percentage is currently treated as a flat amount.
- Labels: bug, posthog-watcher:security-review
- PR: (none)
- Closed: no
- Updated: 2026-06-23T13:45:44.097Z

```json
{
  "triage": {
    "conclusion": "Valid bug: the discount percentage is currently treated as a flat amount.",
    "summary": "The /discount endpoint returns 240 for price=250&percent=10 because calculateDiscountedPrice subtracts 10 directly instead of subtracting 10% of 250.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I verified this against the repository code: `src/business.js` currently returns `price - discountPercent`, so `250` with `10` becomes `240`. There is also an existing test in `test/business.test.js` expecting `calculateDiscountedPrice(250, 10)` to return `225`, matching the behavior described here.\n\nA minimal fix would be to calculate the discount amount as `price * (discountPercent / 100)` and subtract that, preserving the existing rounding behavior.",
    "investigation": {
      "relevantFiles": [
        "src/business.js",
        "src/app.js",
        "test/business.test.js",
        "README.md",
        "package.json"
      ],
      "findings": [
        "`src/app.js` routes `GET /discount` to `calculateDiscountedPrice(price, percent)` using the query parameters from the request.",
        "`src/business.js` implements `calculateDiscountedPrice` as `roundToCents(price - discountPercent)`, which treats the percent as a flat currency amount.",
        "`src/business.js` also contains an inline comment stating this is a bug: it treats a percentage discount as a flat currency amount.",
        "`test/business.test.js` already includes a test expecting `calculateDiscountedPrice(250, 10)` to equal `225`.",
        "`README.md` lists this as a known dogfood bug: discount calculation treats the percent as a flat amount."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The bug is isolated to one helper function, the expected behavior is clearly specified by the issue and an existing test, and the endpoint already passes the percentage value through correctly.",
      "suggestedApproach": "Update `calculateDiscountedPrice` in `src/business.js` so positive discounts subtract `price * (discountPercent / 100)` instead of subtracting `discountPercent` directly, keeping the existing `roundToCents` behavior.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "security": {
    "sensitive": true,
    "reasons": [
      "rce"
    ]
  }
}
```
