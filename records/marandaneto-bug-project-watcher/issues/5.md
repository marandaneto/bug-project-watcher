# issue 5: Discount API is not applying percentage values correctly

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/5
- Conclusion: Valid bug: percentage discount is treated as a flat subtraction
- Labels: bug, good-first-issue, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/6
- Closed: no
- Updated: 2026-06-23T13:53:54.121Z

```json
{
  "triage": {
    "conclusion": "Valid bug: percentage discount is treated as a flat subtraction",
    "summary": "The /discount endpoint delegates to calculateDiscountedPrice, which currently subtracts the percent value directly from price instead of calculating price * percent / 100.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug",
      "good-first-issue"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I verified this against the code: `src/app.js` passes the `price` and `percent` query params into `calculateDiscountedPrice`, and `src/business.js` currently returns `price - discountPercent`. That matches the reported `250 - 10 = 240` behavior instead of a 10% discount (`250 - 25 = 225`).\n\nThere is also already a focused test in `test/business.test.js` expecting `calculateDiscountedPrice(250, 10)` to equal `225`, so this should be a small, well-scoped bug fix.",
    "investigation": {
      "relevantFiles": [
        "src/business.js",
        "src/app.js",
        "test/business.test.js",
        "README.md",
        "package.json"
      ],
      "findings": [
        "`src/app.js` handles `GET /discount` by parsing `price` and `percent` query parameters and returning `{ price: calculateDiscountedPrice(price, percent) }`.",
        "`src/business.js` implements `calculateDiscountedPrice(price, discountPercent)` as `roundToCents(price - discountPercent)`, so a `percent=10` input subtracts 10 currency units rather than 10 percent.",
        "`test/business.test.js` contains a focused expected-behavior test: `calculateDiscountedPrice(250, 10)` should equal `225`.",
        "`README.md` explicitly lists this as a known dogfood bug: discount calculation treats the percent as a flat amount."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The bug is isolated to one business helper, the expected behavior is unambiguous, and a targeted test already documents the correct output.",
      "suggestedApproach": "In `src/business.js`, change the positive-discount branch to compute the discount amount as `price * (discountPercent / 100)` and return `roundToCents(price - discountAmount)`. Then run `npm test`.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "security": {
    "sensitive": false,
    "reasons": []
  }
}
```
