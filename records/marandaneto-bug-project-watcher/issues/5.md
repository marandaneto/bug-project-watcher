# issue 5: Discount API is not applying percentage values correctly

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/5
- Conclusion: Valid bug: the discount endpoint treats the percent value as a flat amount instead of a percentage.
- Labels: bug, posthog-watcher:security-review
- PR: (none)
- Closed: no
- Updated: 2026-06-23T13:46:30.650Z

```json
{
  "triage": {
    "conclusion": "Valid bug: the discount endpoint treats the percent value as a flat amount instead of a percentage.",
    "summary": "Calling /discount?price=250&percent=10 returns 240 because calculateDiscountedPrice subtracts 10 directly rather than subtracting 10% of 250.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I verified this in the repository: `src/app.js` passes the `percent` query parameter into `calculateDiscountedPrice`, and `src/business.js` currently returns `roundToCents(price - discountPercent)`. That makes `250` with `10` return `240`, matching the reported current behavior.\n\nThere is already a test in `test/business.test.js` expecting `calculateDiscountedPrice(250, 10)` to return `225`, and `README.md` lists this as a known dogfood bug. A minimal fix would be to compute the discount amount as `price * (discountPercent / 100)` and subtract that while keeping the existing `roundToCents` behavior.",
    "investigation": {
      "relevantFiles": [
        "src/business.js",
        "src/app.js",
        "test/business.test.js",
        "README.md",
        "package.json"
      ],
      "findings": [
        "`src/app.js` handles `GET /discount` by reading `price` and `percent` from query parameters and passing them to `calculateDiscountedPrice(price, percent)`.",
        "`src/business.js` implements `calculateDiscountedPrice` as `roundToCents(price - discountPercent)`, so the percent is treated as a flat currency amount.",
        "`src/business.js` contains an inline comment noting the bug: it treats a percentage discount as a flat currency amount.",
        "`test/business.test.js` includes a test expecting `calculateDiscountedPrice(250, 10)` to equal `225`.",
        "`README.md` lists this as a dogfood issue: discount calculation treats the percent as a flat amount."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The bug is isolated to one helper function, the expected behavior is explicit in the issue and existing test, and the API route already passes the percentage value through correctly.",
      "suggestedApproach": "Update `calculateDiscountedPrice` in `src/business.js` so positive discounts subtract `price * (discountPercent / 100)` instead of subtracting `discountPercent` directly, preserving `roundToCents` and the existing zero-or-negative discount behavior.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "security": {
    "sensitive": true,
    "reasons": [
      "security",
      "rce"
    ]
  }
}
```
