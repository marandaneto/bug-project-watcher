# issue 28: Shipping endpoint truncates cents

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/28
- Conclusion: Confirmed bug: shipping estimates are floored, dropping cents.
- Labels: bug, good-first-issue, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/33
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28047000896
- Updated: 2026-06-23T18:13:45.308Z

```json
{
  "triage": {
    "conclusion": "Confirmed bug: shipping estimates are floored, dropping cents.",
    "summary": "The /shipping endpoint delegates to estimateShipping, which computes 7.75 for 2.2kg but returns Math.floor(estimate), producing 7.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug",
      "good-first-issue"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear repro. I confirmed this maps directly to `src/business.js`: `estimateShipping(2.2)` calculates `5 + 2.2 * 1.25 = 7.75`, but then returns `Math.floor(estimate)`, which truncates it to `7`. There is already a focused test in `test/business.test.js` expecting `estimateShipping(2.2)` to return `7.75`.\n\nA minimal fix would be to preserve cents using the existing `roundToCents` helper instead of `Math.floor` in `estimateShipping`.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` handles `GET /shipping` by parsing `weightKg` and returning `{ shipping: estimateShipping(weightKg, expedited) }`.",
        "`src/business.js` computes shipping as `5 + weightKg * 1.25`, optionally multiplies by `1.5` for expedited shipping, then returns `Math.floor(estimate)`.",
        "For the reported repro, `5 + 2.2 * 1.25` equals `7.75`, and `Math.floor(7.75)` returns `7`, matching the reported actual result.",
        "`test/business.test.js` already contains `shipping estimate preserves cents` asserting `estimateShipping(2.2) === 7.75`.",
        "`README.md` lists this as a known dogfood bug: shipping estimates round down instead of preserving cents."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The root cause is isolated to one return statement in `estimateShipping`, and the repository already has a helper for rounding to cents plus a focused test for the expected behavior.",
      "suggestedApproach": "In `src/business.js`, replace `return Math.floor(estimate);` with `return roundToCents(estimate);`. Verify with the existing shipping test; note that unrelated intentionally failing tests may still fail until their separate bugs are fixed.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "2890b411687e73a7cbf66f06b9e4b112a98ea3910b49d43871c621c4e9b9f45c",
  "piCalls": 3,
  "runId": 28047000896,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28047000896"
}
```
