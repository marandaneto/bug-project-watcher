# issue 28: Shipping endpoint truncates cents

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/28
- Conclusion: Confirmed bug: shipping truncates cents by flooring the calculated estimate.
- Labels: bug, good-first-issue, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/33
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28363722282
- Updated: 2026-06-29T10:01:56.774Z

```json
{
  "triage": {
    "conclusion": "Confirmed bug: shipping truncates cents by flooring the calculated estimate.",
    "summary": "The /shipping endpoint returns 7 for weightKg=2.2 because estimateShipping uses Math.floor instead of preserving cents; expected result is 7.75.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug",
      "good-first-issue"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed. `GET /shipping` delegates to `estimateShipping`, which calculates `5 + weightKg * 1.25` and then returns `Math.floor(estimate)`, dropping cents. For `weightKg=2.2`, the computed value is `7.75` but the function returns `7`.\n\nA minimal fix is to replace the final `Math.floor(estimate)` in `src/business.js` with the existing `roundToCents(estimate)` helper. There is already a focused test in `test/business.test.js` expecting `estimateShipping(2.2)` to return `7.75`.\n\nNote: there is an open related PR: https://github.com/marandaneto/bug-project-watcher/pull/33",
    "investigation": {
      "relevantFiles": [
        "src/business.js",
        "src/app.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` handles `GET /shipping` by parsing `weightKg` and returning `{ shipping: estimateShipping(weightKg, expedited) }`.",
        "`src/business.js` computes shipping as `5 + weightKg * 1.25`, optionally applies an expedited multiplier, then returns `Math.floor(estimate)`.",
        "For the reported repro, `5 + 2.2 * 1.25` equals `7.75`, and `Math.floor(7.75)` returns `7`, matching the reported actual behavior.",
        "`test/business.test.js` contains `shipping estimate preserves cents`, asserting `estimateShipping(2.2) === 7.75`.",
        "`README.md` lists this as a known dogfood bug: shipping estimates round down instead of preserving cents."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The behavior is isolated to one return statement in `estimateShipping`, and the repository already has a cents-rounding helper plus a focused test for the expected output.",
      "suggestedApproach": "In `src/business.js`, replace `return Math.floor(estimate);` with `return roundToCents(estimate);`. Verify with the existing shipping test; unrelated intentionally failing tests may still fail.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 33,
      "type": "pull_request",
      "state": "open",
      "title": "Fix #28: Shipping endpoint truncates cents",
      "url": "https://github.com/marandaneto/bug-project-watcher/pull/33",
      "labels": [],
      "bodyExcerpt": "Fixes #28\n\nGenerated by posthog-watcher-action using pi.\n\n## Summary\n\nThe /shipping endpoint delegates to estimateShipping, which computes 7.75 for 2.2kg but returns Math.floor(estimate), producing 7.\n\n## Triage rationale\n\nThe root cause is isolated to one return statement in `estimateShipping`, and the repository already has a helper for rounding to cents plus a focused test for the expected behavior.\n\n## Changed files\n\n- `src/business.js`\n\n## Validation\n\n- `node -e \"require('./src/business'); require('./src/app'); console.log('modules load')\"`\n",
      "createdAt": "2026-06-23T18:13:43Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "2a35244fcdb95605b0ae5115c8816bc2f5769873481a0760d7e2f8243d0baa77",
  "piCalls": 15,
  "runId": 28363722282,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28363722282"
}
```
