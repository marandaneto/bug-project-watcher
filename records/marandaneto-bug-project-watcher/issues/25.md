# issue 25: Discount API subtracts the percent value directly

- Repo: marandaneto/bug-project-watcher
- URL: https://github.com/marandaneto/bug-project-watcher/issues/25
- Conclusion: Valid bug: discount percentage is treated as a flat amount.
- Labels: bug, posthog-watcher:fix-ready
- PR: https://github.com/marandaneto/bug-project-watcher/pull/30
- Closed: no
- Run: https://github.com/marandaneto/bug-project-watcher/actions/runs/28046997851
- Updated: 2026-06-23T18:13:29.590Z

```json
{
  "triage": {
    "conclusion": "Valid bug: discount percentage is treated as a flat amount.",
    "summary": "The /discount endpoint passes percent to calculateDiscountedPrice, which currently subtracts the numeric percent directly from price instead of subtracting that percentage of the price.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed. `GET /discount?price=250&percent=10` routes to `calculateDiscountedPrice(250, 10)`, and `src/business.js` currently computes `price - discountPercent`, yielding `240`. The existing test suite already includes the expected behavior: `calculateDiscountedPrice(250, 10) === 225`.\n\nA minimal fix should update the discount calculation to subtract `price * (discountPercent / 100)` while preserving the existing zero/negative discount behavior and cent rounding.",
    "investigation": {
      "relevantFiles": [
        "src/app.js",
        "src/business.js",
        "test/business.test.js",
        "README.md"
      ],
      "findings": [
        "`src/app.js` handles `GET /discount` by reading `price` and `percent` query params and returning `calculateDiscountedPrice(price, percent)`.",
        "`src/business.js` has `calculateDiscountedPrice(price, discountPercent)` returning `roundToCents(price - discountPercent)`, with a comment noting it treats percentage discount as a flat currency amount.",
        "`test/business.test.js` already asserts `calculateDiscountedPrice(250, 10)` should equal `225`.",
        "`README.md` lists this as a known dogfood bug: discount calculation treats the percent as a flat amount."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The bug is isolated to one calculation in `src/business.js`, with an existing failing test that directly captures the expected behavior.",
      "suggestedApproach": "Change `calculateDiscountedPrice` to compute `price - price * (discountPercent / 100)`, keep the existing `discountPercent <= 0` branch, and continue using `roundToCents` on the result.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "f301e428ed706c033c419e2aefb8072a022e592d5952c321e0dae4afe506aac8",
  "piCalls": 3,
  "runId": 28046997851,
  "runUrl": "https://github.com/marandaneto/bug-project-watcher/actions/runs/28046997851"
}
```
